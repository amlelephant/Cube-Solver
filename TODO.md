- [ ] Anticheat test and finlization. This will most likely include recording sessions where cube swaps occur in order to figure out how to prevent it. We still struggle with the issue of the table swap where the edge of the table is mid frame and you swap from below. An idea I had for profitablility was to charage a subscription service to actually record the moves of a solve. If the user dosen't pay for this service then the only feature they get is verification (which is still novel). This could lead to an issue where we need to tighten the anticheat. The original proposed solution after the most recent session was to use the move detector/classifier model to verify that the moves actually occur so without it there would be an issue. 
- [ ] Django backend build out. We still need to impliment the plan that is in SYSTEM_DESIGN.md. This plan outlines how we will use django with the reach front end to create a well developed user experience. More info about this can be found in the aforementioned doc. 
- [ ] Backtracker redesign (or fix). The backtracker is somewhat useless right now. It can do tiny tweaks but it overall breaks down at the level of error we are encountering (obviously because there are so many outcomes it is likely not possible) so we need to approach it in a different way. I still don't believe that the algorithim method was implimented correctly. It seems to not have cleared up any real issues with the algorithims. The biggest issues with the algorithims is not the correct identification but insertions and deletions. The onset detector is missing moves or possibly merging them together if the onset peak is too close in time to another (which could be possible since the algos are fast).
- [ ] Full move detection finalization. This bullet is vauge but needs to be accomplished. This has been the primary focus of this entire project so I will likely steer the ship on this one. We have a relly good all in one model infrustructure right now but it still isn't good enough. We are sitting at around 80% accuracy total. This is really good and better than anything out there but we are really in new territory now.  This part is slightly optional for mvp but it would be good to go into this with the possibiility of making money (because this is the only part I was planning on charging for). First goal needs to be actually knowing what our goal is. We need to test what % accuracy we need to actually end up with a verification on, lets say 90% of solves. We don't need verification for it to be a confirmed win (see task 1 anticheat) but we do need it so the user is confident in the data they get (they are paying afterall). This will set our goal and give us a clearer path forward. Another issue that needs to be addressed is it testing so well in environments of same day to training but failing on different conditions (even in the same spot). It seems like since the cube is bounded by a detector it wouldn't be effected by this (except for lighting). It seems as if we solve the issue between different environments we can extrapolate the same environment results to the entire model. At 97% accuracy I'm almost certain we can verify most of the solves. 
- [ ] Add functionality for the backtracker to be able to infrence cube rotations and where they happened. If this is shown to be impossible (within edit budget) we should consider also making a rotation detector model. It seems as if it would be very light weight but introducing false positives could make our job more complicated rather than less. The product is not finished untile we confirm that it works on cube rotations because we cannot expect users to not rotate the cube if they want valid verifications. (Their moves will still be recorded correctly but we still need a way to verify)